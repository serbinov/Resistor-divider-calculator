# Калькулятор резистивного делителя — Описание и план работ

## Краткое описание

Это веб‑приложение — калькулятор двухрезисторного делителя напряжения (R1 — верхний, R2 — нижний). Инструмент позволяет:

- Рассчитать соотношение R1/R2 по заданным Vin и Vout (режим Auto).
- Подобрать стандартные номиналы резисторов из рядов E24, E96 и сгенерированного E192.
- Ввести один известный резистор (R1 или R2) и получить второй, или ввести оба вручную.
- Учитывать нагрузку (Rload) и показывать загруженное и свободное/открытое Vout.
- Оценивать мощность на каждом резисторе, токи и рекомендовать корпус/мощность.
- *Removed*: Candidate resistor pairs table (previously top‑10 selection) has been removed; users now set a resistor search range instead.
- Поддерживать опцию поиска комбинаций из двух резисторов (серии для R1, параллель для R2) для улучшения соответствия.
- Таблица стандартных рядов с возможностью копирования номинала и установки значения в R1/R2.

---

## Что реализовано (подробно) ✅

1. Перенаправление модели:
   - Перевод с модели «Vref + feedback» на общую модель делителя: Vout = Vin × R2 / (R1 + R2).
   - По умолчанию Vin = 5.000 V, Vout = 3.3 V.

2. Интерфейс и режимы:
   - Три режима: Auto (подбор), Single (задан один резистор), Manual (вручную).
   - Поля ввода: Vin, Vout, Rload (опционально, kΩ), ppm для R1/R2, выбор ряда резисторов. (Поле `Vin Tolerance` удалено — не учитывается в расчётах.)

3. Ряды номиналов и точность:
   - E24 (5%), E96 (1%), сгенерированный E192 (используется для 0.5%, 0.25% и 0.1%).
   - Селектор «All» для поиска по E192 + E96 + E24.

4. Алгоритм подбора:
   - Подбор значения по выбранной стратегии (Auto/Single/Manual), с учётом заданного диапазона допустимых резисторов (min/max в kΩ, по умолчанию 1–100 kΩ).

5. Учёт нагрузки (Rload):
   - Если задан Rload, используется эквивалент R2 || Rload для расчёта загруженного Vout.
   - В результатах показаны: Vout (загруженный), Vout (откр. цепь) и ток через нагрузку.

6. Комбинации резисторов (опция):
   - По запросу выполняется ограниченный поиск двухкомпонентных комбинаций:
     - Для R1: сумма двух стандартных номиналов (серийно) — пытаемся улучшить R1.
     - Для R2: параллель двух стандартных номиналов — пытаемся улучшить R2.
   - Комбинации резисторов поддерживались ранее, но возможность просмотра кандидатов была удалена; ограничение числа проверок при поиске комбинаций по-прежнему учитывается в алгоритме для производительности, если используется внутренняя оптимизация.

7. UI результатов и вспомогательные функции:
   - Блок результатов с подробными расчётами: мощность, токи, температурная и допусковая вариации.
   - Примечание: таблица кандидатов и связанные с ней кнопки «Use/Details» были удалены из UI.
   - Top combinations: shows a compact summary for the top candidate resistor sets (up to 3). After each resistor nominal the card shows the standard series and tolerance in green, Vout with error, and Min/Max worst-case values (tolerance+temperature); the block is compact to avoid excessive duplication with the detailed Results block.
   - Копирование значения → немодальное уведомление (всплывашка).

8. Доп.функции инженерной поддержки:
   - Рекомендации по корпусам SMD/Through‑hole на основе расчетной мощности и рабочего напряжения.
   - Рассчитываются вариации выходного напряжения по допуску резисторов, PPM температурного дрейфа и допуску Vin.

9. Технические файлы:
   - Функции: calculateResistors(), findClosestResistor(), generateESeries(), augmentCandidatesWithCombos(), renderSeriesTable(), applyCandidate(), и др.
   - Единственный рабочий файл: `index.html`. (Дополнительные файлы-редиректы были удалены.)

---

## Что нужно сделать дальше (приоритеты) ✳️

Высокий приоритет (рекомендуется сначала):

1. UX/полировка (легкий, 1–2 часа)
   - Добавить индикатор прогресса при длительном поиске (комбинации) и кнопку отмены.
   - Улучшить сообщения об ошибках и валидацию входов.

2. Тестирование и верификация (средний, 2–4 часа)
   - Набор простых unit‑tests для core функций (подбор резисторов, генератор E192, расчёты мощности).
   - Кросс‑проверки с ручными примерами: 5V→3.3V, 12V→5V, Rload cases.

3. Экспорт и сохранение (средний, 2–4 часа)
   - Экспорт результатов / таблицы кандидатов в CSV.
   - Сохранение истории в localStorage и восстановление предыдущих расчётов.

4. Документация (низкий, 1–2 часа)
   - Обновить README.md, добавить примеры использования и пояснения к параметрам.
   - Добавить короткий раздел по тестированию и способам проверки результатов.

Средний/низкий приоритет (по мере необходимости):

5. Расширенный поиск комбинаций (тяжёлый)
   - Допускать 2‑резистора для обеих сторон (R1 и R2 одновременно) — потребуется более тонкая стратегия поиска и/или бэкграунд‑таски.

6. Экономические/поставочные аспекты
   - Предлагать ближайшие доступные значения из популярных поставщиков (Mouser/SMT) — потребует подключения API или локальной базы данных.

7. Доп. инженерные расчёты
   - Оценка теплового режима при длительной работе, шум (Johnson‑Nyquist), частотная зависимость при высокочастотных применениях.

---

## Как протестировать (базово)

1. Откройте `index.html` в браузере.
2. Примеры для проверки:
   - 5 V → 3.3 V (стандартный кейс). Проверьте, что в результатах есть кандидат с ошибкой < 0.5%.
   - 12 V → 5 V. Установите Rload = 10 kΩ и проверьте, что Vout (loaded) отличается от свободного значения.
   - (Note: 2-resistor combos are not displayed in the UI anymore.)
3. Попробуйте режим Single (известный R1 или R2) и Manual.

---

## Файлы и функции (краткий список)

- `index.html` — главный файл приложения (всё в одном). Основные функции:
  - calculateResistors() — основная логика подбора и рендеринга результатов.
  - findClosestResistor(value, tolerance) — ближайшее стандартное значение.
  - generateESeries(m, sig) — генерация E192.
  - (augmentCandidatesWithCombos helper removed — advanced combo search is not used in the UI).
  - (renderSeriesTable and the "Standard resistor series" block have been removed from the UI).
  - renderSeriesTable() — вывод стандартных рядов и операций Copy/Use.

---

## Примечания и рекомендации

- Поиск комбинаций потенциально дорогостоящ — реализовано с лимитом trial'ов и по запросу (checkbox). Можно добавить прогресс‑индикатор и фоновые задачки (Web Worker) при необходимости.
- Исправлено: добавлено поле `Rload` в UI и добавлены защитные проверки в `calculateResistors()` чтобы избежать ошибки при отсутствии поля (фикс: "Cannot read properties of null (reading 'value')").
- Код централизован в одном файле для простоты деплоя; при расширении логично вынести модули в отдельные JS‑файлы и добавить минификацию для продакшена.

---

Если хотите, могу: 
- добавить unit‑tests для ключевых функций, 
- реализовать экспорт в CSV и history в localStorage, 
- или подготовить релиз‑README и короткие примеры использования. 

Скажите, какой следующий приоритет выполнить, и я начну работу. 🚀